// Holds current value of what is pressed down.
var isKeyDown = {
    leftKey:false,
    rightKey:false,
    upKey:false,
    dotKey:false,
    commaKey:false,
    aKey:false,
    dKey:false,
    wKey:false,
    cKey:false,
    vKey:false,
};

// Called everytime key is pressed.
document.addEventListener('keydown', function(event) {
  // TODO: Add states to hold keys.
});
